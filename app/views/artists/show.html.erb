

<h1><%= @artist.name %></h1>

<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, song_path(song) %>:<%= pluralize(@artist.songs.count, 'Song') %></li>
    
  <% end %>
</ul>

